# Local .terraform directory (contains provider binaries and modules)
.terraform/

# .tfstate files (remote backends should be used instead)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files (likely to contain sensitive secrets/passwords)
*.tfvars
*.tfvars.json

# Ignore override files used for local resource adjustments
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock files created by terraform apply
.terraform.tfstate.lock.info

# Ignore generated plan files (may contain sensitive data)
*.tfplan

# Ignore CLI configuration files
.terraformrc
terraform.rc
*.plan
tfplan.txt
